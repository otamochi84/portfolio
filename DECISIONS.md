# 設計上の決定

このサイトが**なぜそうなっているか**の記録。同じ議論を何度も繰り返さないために残している。

**実装の詳細（どう書いてあるか）はコードのコメントに書いてある。** ここに残すのは、コードを読んでも分からない「なぜそう決めたか」だけ。

---

## サイト構成

### Worksは「実績一覧」ではなく「活動カテゴリ3枚のカード」

2026-08-04に方針を変えた。もとは案件ごとの実績を並べる形だった。

**理由は運用の負担。** 案件ごとにサムネイル画像を作るのが続かなかった。カテゴリ3枚なら画像は3回作れば済み、1枚1枚を作り込む余裕もできる。

あわせて `/service` を廃止した。Notionのことしか書いておらず、文脈が薄かったため。本文はNotionのカテゴリページへ移した。

個別の実績は `/projects/[スラッグ]` に残っている。カテゴリページからテキストで一覧できる。

### サイトの中身はすべてNotionが正（2026-09-12）

**NotionがこのサイトのCMS。** コード側に原稿や設定値を置かない。WordPressのような管理画面を持たずに済ませているのは、使い慣れたNotionでそのまま書けることが利点だからで、**その利点はコードに原稿が散った瞬間に消える。** 二重管理は必ず片方が腐る（実際に腐った例は下の「本文と経歴の予備原稿はコードに持たない」）。

`HP_Contents` の「用途」で引く。用途名は英小文字・ハイフン区切りで既存に揃える。

| 用途 | 何が変わるか |
|---|---|
| `hero-kv` / `about-photo` | キービジュアル・スナップ写真 |
| `about-jp` / `about-en` | About本文 |
| `background` | 経歴 |
| `site-title` | 全ページのタブ名・検索結果の見出し・SNSカードの見出し |
| `meta-description` | トップの検索結果の説明文 |
| `journal-description` | Journal一覧の検索結果の説明文 |
| `contact-url` | 全ページのContactボタンのリンク先 |
| `favicon` | タブのアイコン |

**コード側に残してよいのは**、画面の部品（セクション見出し、`View all`、`Category：` などのラベル）と、変えないと決めた固定値（名前・屋号、Journalカードの上限7枚）だけ。

取り出し口は `getSiteMeta()` 1つに寄せている。各ページで用途キーを直接書くと、キー名が1文字違うだけで静かに空になり、変更のたびに全ページを直すことになるため。

- **`.ico` はNotionに置けない。** ファイルアップロードAPIが `image/x-icon` を受け付けないため、SVGを1枚だけ登録し、**32×32のPNGはビルド時にsharpで生成**している。PNGも別途アップすると、差し替えのたびに2枚を揃える二重管理になる
- **経歴の和文・英文は行の位置で判定しない。** Notion側で書く順を入れ替えても表示が入れ替わらないよう、日本語の文字を含むかどうかで振り分ける（2026-09-12に、`[year, en, jp]` と位置で決め打ちしていたのを直した）
- **OGPのカード画像は専用画像を作らない。** トップはキービジュアル、Journal記事は記事のサムネイル、Works/Projectsはカテゴリ画像を使い回す。Journal一覧は共有しないので持たない。画像はWebPになるが、共有先として想定しているXとFacebookはどちらも対応している

---

## デザインの決めごと

### 背景は2色の交互だけ。黒背景は使わない

ベース `#f8f8f8` とセクション `#fff` の交互で組む。**黒は文字色としてのみ使う。** かつてContactが黒背景だったが廃止した。

### アクセント色はテラコッタ

文字 `#b3592f` ／ 面 `#f7e7de`。薄いオレンジ系。ここぞという箇所だけに使い、多用しない。

### フォントの役割分担

新しい要素を足すときはこの割り当てに従う。

| 役割 | フォント |
|---|---|
| 屋号（Intro / About） | Comfortaa 700 |
| Welcome! | Quicksand 700 |
| セクション見出し・ナビ・数値 | Jost（`Futura` のフォールバック先） |
| 和文本文 | Zen Old Mincho 400 |
| 欧文本文 | EB Garamond 400 |
| Journalタイトル | Noto Sans JP 400 |

**例外: Backgroundの年号は EB Garamond**（2026-09-11）。「数値はJost」から外している。ここの年号は独立した数値ではなく、下に続く英文表記と一組で読まれる並びのため。これで Background 内の書体が和文・欧文の2種に収まる。Journalの日付（`.jrow-date` / `.jgal-date`）は今までどおりJost。

**具体的な数値（余白・文字サイズ・角丸・イージング）はCSSが正。** ここには書かない。二重に持つと必ず片方が古くなるため。

### JournalのVIEW ALLは横スクロールの末尾に置く（2026-08-15）

カードの下に置いていた「View all」を廃止し、カード列の末尾へ移した。**最後まで送りきった人にだけ現れる**のが狙い。額装なしのテキストで、画面に入ってきたところで浮かび上がる。

カードの上限は7枚。超えたぶんは `/journal/` で見せる。

- カード型（他のカードと同じ額装で並べる案）と一時トグルで見比べてテキスト型を採った
- フェードインは0.7秒だと速く感じたので1.1秒にした。「最後まで送りきったご褒美として出すので、急いで現れるよりゆっくり浮かび上がるほうが合う」
- **`scroll-snap-type: x mandatory` でもスクロールは終端に到達する。** 最後のスナップ位置が最大スクロール量を超える場合、ブラウザが末尾にクランプするため。計算だけで「終端に届かない」と判断して一度間違えたので記録しておく（Playwrightで `reachedEnd: true` を実測済み）

### 本文の折り返しはCSSで決める。原稿に手で改行を入れない（2026-09-11）

**Notion側のAbout本文は「1段落=1文・手置き改行なし」で書く。** `<br>` で改行位置を固定すると画面幅で崩れる。以前は英文を7行に手置きしていたが、左カラムの最大幅が437.6pxしかなく、最長行409pxが収まるのは1382px以上の画面だけだった。ノートPCの主力である1280/1366pxでは常に崩れていた。

**`text-wrap: pretty` は本文に効かせない。** bodyには掛けたままだが `.body-jp` / `.body-en` で打ち消している。**Safari 26 の `pretty` は段落全体を見て行分割を最適化するため、行末に届く前に折り返して右側に空白を作る**（実測: 和文の行末の余りが13px→79pxに増えた）。Chromiumは有無で結果が変わらないので打ち消してもデメリットがない。打ち消すと両エンジンの行分割が1文字違わず一致する。ブラウザによって見え方が違うと編集方針が立てられないため、一致を優先した。

**和文は `word-break: keep-all` と `<wbr>` で文節折り返しにする。** 既定の日本語の折り返しは字数だけで切るので「アトツ／ギ支援」のように語が割れる。`index.astro` の `splitForWrap()` が助詞・読点の直後（かつ次が漢字かカタカナの位置）に `<wbr>` を置き、CSSがそれ以外での折り返しを禁じる。**この方式なら原稿を書き換えても折り返しは自動で計算し直されるので、編集のたびに確認する必要はない。**

- **`word-break: auto-phrase` は使えない。** 文節で折り返す標準機能でこれが本来の正解だが、**WebKitが未対応**（`CSS.supports('word-break', 'auto-phrase')` が false）。Chromiumだけ効くと表示が食い違うため採らなかった。将来Safariが対応したら `splitForWrap()` ごと置き換えられる
- **`text-wrap: balance` も不採用。** 両エンジンで一致はするが、行長を均等化する都合で「山椒や／梨」「研究／開発」のように語が割れる
- 形式名詞（後・機・際・上・中・時・方・頃・間）の手前は候補から外している。入れないと「その／後」「結婚を／機に」と割れる
- **`text-align: justify` は入れて外した。** 折り返し位置を固定すると、justifyが効く行（段落の途中）と効かない行（各段落の最終行）とで字間が不揃いになり、かえって目立つ
- 逃げ道として `overflow-wrap: anywhere` を併用している。助詞も読点も挟まない20文字超の連続語（長いカタカナ語など）が1行に収まらないときだけ働く

### 本文と経歴の予備原稿はコードに持たない（2026-09-12）

`index.astro` には長らく About 本文（`aboutProfile.jp` / `.en`）と経歴（`backgroundFallback`）の予備原稿が書いてあり、Notionから取得できないときだけそちらが表示される作りだった。**これを削除した。**

理由は二重管理が実際に腐ったため。Notion側の本文を書き換えても予備原稿は誰も直さないので、削除時点では「2025年　結婚を機に奈良へUターン」「現在は香芝市を拠点に活動」といった古い文章が残っていた。経歴も英語表記だけNotionと食い違っていた（`BSc in Agriculture` vs `Faculty of Agriculture`）。つまり**Notionの取得に失敗したときだけ、古い経歴が静かに公開される**状態だった。しかも取得失敗でもビルドは成功扱いになるため、エラーで気づくこともない。

いまは取得できなければ About 本文は描画されず、経歴は0件ならBackgroundの枠ごと出ない（見出しと枠線だけの空箱を避けるため）。**空欄になって異常に気づけるほうが、古い情報が出るより安全**という判断。Notionから取らない固定値は名前と屋号だけ。

### Backgroundは枠線で囲み、箇条書きの記号は使わない（2026-09-11）

**年号が3行とも4桁で縦に揃うので、項目の始まりは年号自身が示せる。** かつて置いていた菱形マークは同じ役目の二重化だったため外した。

枠線は**右端を写真の右端に揃える**（`margin-right: 0`）。色はテラコッタを55%まで透かした `rgba(179, 89, 47, 0.55)` の1pxで、角丸は4px。不透明のまま使うと見出しと同じ濃さになり、従であるはずの枠のほうが目立つ。角丸を8pxまで丸めると丸みのほうが主張する。

見出しは枠線の上辺に重ね、文字の背後をセクションの白で塗って線を切っている（一筆書きの切り欠き）。上辺の左に残す線は枠内余白の半分。全部覆うと左上の角が欠けて見え、覆わないと線が長い。**見出しの `display` は `block`。** `inline-block` だと親の行ボックスの高さに引きずられて中心が上辺から下にずれ、しかもズレ量がエンジンごとに変わった（Chromium 6.00px / WebKit 4.87px）。`block` なら両方1px以内に収まる。

濃さは和文 `#4b5563`、年号と英文 `#6b7280` の2段階。**`#9ca3af` は白背景でコントラスト比 2.54:1 となりWCAG AA（4.5:1）を割るため本文には使わない**（`#6b7280` は 4.83:1、`#4b5563` は 7.56:1）。

---

## 見た目で却下した案

**Worksカードの輪郭の出し方**は、一時的な切り替えスイッチを仕込んで実機で見比べながら決めた（2026-08-11）。採用したのは「生成り `#faf8f4` ＋ にじむ影2枚、枠線なし」。

以下は検討して**却下した**もの。蒸し返さないこと。

| 案 | 却下理由 |
|---|---|
| **織り目テクスチャ**（画布の目をCSSで描く） | 等間隔の格子は織り目ではなく**方眼紙**に見える。「方眼紙に絵は描かない」。本物の織り目は不規則で、CSSの規則的な格子では原理的に再現できない |
| **イーゼルの線**（受け木と脚を描く） | イーゼルに見えなかった |
| **四隅のコーナーマーク**（トンボ風） | 好みに合わなかった |
| **罫線グリッド**（カードを囲まず全体を表に見せる） | 実装前に却下。余白たっぷりの基調と喧嘩する／「額装された作品」の見立てが消える／カードが2〜3枚では表に見えない |
| **ずらし影**（ぼかしゼロ・8px） | 装飾が渋滞して見える。3px・5pxまで落とした版も比較したが、最終的に影なしを採用 |

### Journalで却下した案（2026-08-15）

| 案 | 却下理由 |
|---|---|
| **VIEW ALLのカード型**（他のカードと同じ額装で並べる） | 一時トグルでテキスト型と実機比較して決めた。**言語化された決め手はなく、見た目の好み。** 蒸し返すなら同じやり方で見比べること |
| **横スクロール端のにじみ**（エッジフェード） | スクロールできる側の端だけをにじませ、端まで来たら消える案。scroll-driven animationsで実装して比較したが、**あり／なしの違いが分からなかった** |

#### エッジフェードを再検討するとき用のメモ

却下したときに調べたブラウザ対応（2026-08-15時点）。次に似た検討をするときの前提になる。

| 機能 | 状況 |
|---|---|
| `animation-timeline: scroll()` | Chrome 115〜（2023.7）／**Safari 26〜（2025.9）**／Firefoxは152時点でもフラグの裏 |
| `@property` | Baseline 2024。全ブラウザ対応 |
| `::scroll-marker()` / `::scroll-button()` | **not Baseline。まだ使えない**（ドット・矢印を純CSSで出す機能） |

実装の参考にしたのは `trenaryja/ui` の `src/css/overflow-fade.css`（`gh search code` で発見）。**要素自身に `scroll-timeline` を宣言して自分で参照すれば `timeline-scope` は要らない。** `@property` の `initial-value: 0` が、非対応ブラウザでの「にじみなし」フォールバックを兼ねる。

`GoogleChromeLabs/css-web-ui-demos` にAstro製のカルーセルデモが17種あるが、中身は `::scroll-button()` 依存でChrome専用。Safari対応後に見る価値がある。

### 写真を「手書き風」に加工するのは採らない

CSSフィルタやSVGフィルタで写真を手書き風にすると**品質が出ない**。安っぽい加工に見える。

ただし、Notionカテゴリの画像に**手描きイラストそのものを使えば**、CSSの小細工なしで「絵」に見える（Notion Consultingのカードが実例）。この方向で画像を揃えるほうが確実。

### カードが2枚のときの3列目の空きは、左寄せのままでよい

2026-08-09に決着。中央寄せにする案（`:has()` で列数を枚数に追従させる）を実際に実装して実機で見比べた結果、**左寄せのほうが見た目が良かった**ので撤回した。

---

## 画像の扱い

**この件は過去に2回、同じ議論を繰り返している。** 結論を残す。

### アップされた画像の寸法は縮小しない

大橋さんの前提が「アップ前にサイズ調整するのは手間」「画質は落としたくない」であるため。`webpQuality` を下げるのも画質低下なので不可。

そのうえで軽くするために、**Worksカードにだけ srcset を入れている**（実装は `src/lib/notion.ts` の `downloadCardImage()`）。原寸はそのまま残し、800px版・1200px版を追加で作ってブラウザに選ばせる方式。

実測では、モバイルで **952KB → 187KB（80%減）**。表示サイズに合う解像度を渡しているだけなので画質の劣化はない。

| | srcset対応 |
|---|---|
| Worksカード | **する**（800 / 1200 / 原寸） |
| キービジュアル・About写真・本文画像 | **しない。** 全幅〜記事幅で表示するので原寸が適正 |

### 小さい画像がアップされたときの周知は、Notion側に書く

長辺1200px未満だと表示時に引き伸ばされて粗くなる。この注意書きは、**Notionの親ページ「HP」→ `HP_Works` →「📝 プロパティ構成」の表に書く**方式にした。

検討して却下した案:

- **プロパティの説明欄に書く** → NOTION.mdの規則「データベースのプロパティに説明を付けない」に反する
- **ビルドログに警告を出す** → 大橋さんがビルドログを読む運用ではない
- **ビルド時にNotionへコメントを投稿する** → ビルドごとにコメントが溜まる（重複排除の実装が必要）／ビルドが書き込み処理を持つ設計になる／気づくのがアップ後になる。そもそも起こりにくい事象なので手間に見合わない

---

## Instagram連携は見送った（2026-08-13）

**Journalは今後もNotion（`HP_Journal`）で運用する。**

決め手は「Instagram APIを使うにはFacebookページの作成と運用が必要」だったこと。**Instagramの更新を楽にするために、使わないFacebookページの運用が増えるのは本末転倒**という判断。

当初の目的だった「画像ベースの綺麗な見た目」は、`HP_Journal` に画像プロパティを足して横スクロールギャラリーにすることで達成済み。

### 調べて分かったこと（将来また検討するとき用）

- **App Reviewは不要。** 自分のアカウントだけなら Standard Access で足りる
- Instagram Basic Display API は2024年12月4日に完全停止。後継は Business / Creator アカウント限定
- トークンの期限は**認証方式で違う**
  - Instagram Login方式 … 60日、要リフレッシュ。**Facebookページ不要**
  - Facebook Login for Business方式 … 長期ページトークンは**無期限**。ただし**Facebookページが必須**
- どちらの方式でも `data_access_expires_at`（約90日でデータアクセスが失効）が別枠で存在する。**トークンは有効なままデータだけ空で返る**ので気づきにくい。「無期限トークン」を紹介する記事はここに触れていないものが多い
- したがって運用は「60日ごとのトークン更新」ではなく**「約90日ごとの再認証」**になる。作業はゼロにならない
- oEmbed は2026年6月15日からトークン不要・App Review不要。ただし返るのはInstagram公式スタイルの埋め込みHTMLで、ギャラリー表示にはならない
- **静的サイトなので、投稿しても次のビルドまで反映されない。** 定期ビルドの仕組みが別途必要になる

---

## Notionの更新を公開する仕組み（2026-08-18）

**Notionを更新しても、ビルドが走らない限りサイトには出ない。** 中身をNotionから取ってくるのはビルドの瞬間だけだから。実際、この仕組みを入れる前は2〜3日分の編集が未反映のまま溜まっていた。

**Cloudflare Pages の Deploy Hook を、Notionのボタンから叩く**方式にした。親ページ「HP」の「サイトに公開」ボタン → `main` のビルドが走る → 数分で公開される。

検討して却下した案:

- **DBオートメーションで自動発火** → 編集途中の保存でもビルドが走ってしまう。条件を詰める手間に見合わない
- **定期ビルド（1日1回など）** → 反映が最大1日遅れる。更新がなくてもビルド枠を消費する

### コミットは増えない

Deploy Hookがやるのは「`main` の最新コミットでビルドをやり直す」ことだけで、gitには何も書き込まれない。**`git log` を見ても「いつ公開したか」は永久に分からない。**

記録はCloudflare側にしか残らないので、`scripts/deploy-log.sh` で読む。`deployment_trigger.type` に `deploy_hook`（ボタン）と `github:push`（push）の区別まで入っている。

### Cloudflare APIトークンは平文でよい

権限を **Cloudflare Pages: Read** だけに絞ったので、漏れても履歴が見えるだけ。サイトの改ざんも課金もできない。Keychainに入れる手間に見合わないため `.env` に平文で置く（`chmod 600`）。

---

## Notionトークンの管理をKeychainへ移した（2026-08-18）

`NOTION_API_KEY` が `.env` に平文・パーミッション644（同じMacの他ユーザーからも読める）で置かれていた。

調べて分かったこと:

- **`ntn`（Notion CLI）とは別のトークンだった。** `ntn` は "Notion CLI"（owner: user）を使い、こちらは元からKeychainで管理されている。`.env` にあったのは "HP連携"（owner: workspace）で、無防備だったのはこちらだけ
- **このキーで `ContactForm` DBが読めた。** 親ページ「HP」にインテグレーションを接続したため、その下にあった問い合わせDB（会社名・お名前・メールアドレス・問い合わせ内容）まで巻き込まれていた。**ビルドでは一度も読んでいない**（サイトはNotionの公開フォームへリンクを貼っているだけ）
- **書き込み権限があった。** 存在しないページIDへのPATCHが403ではなく404を返した＝権限チェックは通過していた

対応:

1. **ContactFormへの接続を外した**（アクセスできるDBが5件→4件になったことを確認済み）
2. **Keychainに移した**（サービス名 `otamochi-portfolio-notion`）。`.env` からは削除
3. **`.env` を `chmod 600` にした**

### 読み取り専用に絞った

インテグレーションの機能を「コンテンツの読み取り」だけにした。**ビルドは読み取りAPIしか使っていないので、絞っても何も壊れない**（絞ったあとビルドが通ることを確認済み）。

効果は検証済み:

| 操作 | 絞る前 | 絞った後 |
| --- | --- | --- |
| 4つのDBの読み取り | 200 | **200**（変わらず） |
| ページの更新要求 | 404（＝権限チェックは通過） | **403 `restricted_resource`** |

これで、万一このキーが漏れても**Notionを書き換えられない**。読めるのもHP配下の4DBだけなので、被害は「すでに公開サイトに出ている情報が読まれる」ところまでに限定される。

**ビルドに `ntn` のトークンを使ってはいけない。** `ntn` は "Notion CLI"（owner: user）でワークスペース全体にアクセスでき、実際に叩くと打ち合わせ記録・請求関連・ContactFormまで100件以上が返る。HP連携キーが存在する意味は「ビルドという自動処理に見せる範囲を4DBに閉じ込めること」にある。

### package.json は書き換えていない

`npm run dev` / `npm run build` をラッパー経由にすると、**Cloudflareの本番ビルドでも同じものが走る**（ビルドコマンドが `npm run build` のため）。Linuxコンテナに `security` コマンドは無いので落ちる。

ラッパー側にも「環境変数が既にあればKeychainを見ない」ガードを入れてあるが、二重の安全のため `package.json` 自体は触らないことにした。ローカルでは `./scripts/with-notion-token.sh astro build` を使う。

---

## 対応しないと決めたもの

### 孤児ファイルの掃除（2026-08-13）

`public/notion-images/` は古いファイルを消さないため、画像サイズの設定を変えると未使用ファイルが残る。**本番はクリーンな状態からビルドするので影響しない。ローカルだけの話。**

対応しない理由:

- 発生するのは**画像処理のコードをいじったときだけ**で、改修が終わればほぼ起きない。実際に確認した時点では孤児0（全19ファイルが現役）だった
- 消したくなったら `public/notion-images/` を手で削除すれば全部落とし直すだけで足りる

**なお「ビルド前に空にするスクリプトを足す」案は採らない。** 差分ダウンロードを導入した今それをやると、ビルドのたびにキャッシュが飛んで差分ダウンロードが無意味になる。

### OGP取得結果の永続キャッシュ

「画像と同じようにOGPもキャッシュすればいいのでは」という案は採らない。

キャッシュの置き場である `public/notion-images/` はgit管理外で、**本番は毎回クリーンビルドなので一度も効かない。** 直したい問題（本番でブックマークのタイトルが崩れる）には無力。だから本番でも効くリトライを選んだ。

そもそもOGPには「リンク先が更新されたか」を判定する材料がない。Notionの `last_edited_time` はブックマークを貼った側の編集時刻であって、リンク先の更新とは無関係。画像の差分ダウンロードと同じ形には作れない。

### 本文ブロックの子要素の並列取得

`getPageBlocks()` の子ブロック再帰は**意図的に直列のまま**にしている。理由はコード側のコメント（`src/lib/notion.ts`）に書いてある。要点は「レートリミットに当たると本文が消えたままビルドが成功してしまう」こと。

---

## 見た目の決め方

### デザイン案は、サイト内に一時トグルを置いて実機で見比べる

複数案があるときは**画面にボタンを置いて切り替えられるようにする。** DevToolsで属性を書き換える方式は却下（「切り替え方法がよくわからない」）。

- `tmpXxx` の接頭辞を付け、HTML / CSS / JS の各1ブロックにまとめる
- `▼▼▼ 一時的な比較用スイッチ … ▲▲▲` のコメントで囲み、機械的に消せる形にする
- 本体のコードには手を入れず、一時コード側だけで完結させる
- **決着したら一時コードを全削除し、負けた案のCSSも消す。** 消したことを `grep` で検証してから報告する。決着前に main へマージしない

前例: Worksカードの輪郭（2026-08-11、`tmpcanvas`、4案）／JournalのVIEW ALL（2026-08-14、`tmpviewall`）／エッジフェード（2026-08-15、`tmpfade`）／Background枠線の濃さと角丸（2026-09-12、`tmpframe`、濃さ4段階×角丸4段階の2軸）。

**確認を求めるときはスクリーンショットを送らず、`./scripts/with-notion-token.sh astro dev` を立ててローカルで見てもらう**（2026-09-11）。位置やサイズの一致は目視ではなくPlaywrightで座標を実測して数値で示す（例:「写真の右端と枠線の右端のズレ 0.00px」）。

### 見た目の検証にはPlaywrightを使う（2026-08-15）

このMacにChrome系ブラウザが1つも無く、実装した見た目を確認する手段がなかった。文字幅を手計算して代用していたが推測が外れることが続いたため、Playwrightの Chromium を devDependencies に入れた。

**ブラウザアプリ自体を増やさない**ことを優先している（`Claude in Chrome` を使う案はChrome本体のインストールが必要になるため見送り）。Chromiumのバイナリは `~/Library/Caches/ms-playwright/` に入るのでリポジトリには含まれない。

これで「アニメーションが実際に何秒で、opacityがどう推移したか」を実測できる。VIEW ALLのフェードインの時間も、これで測りながら決めた。

**スクリーンショットと検証用スクリプトはリポジトリに残さない。** セッションの一時ディレクトリ（`/private/tmp/claude-*/…/scratchpad`）に出して使い捨てる。撮った画像を溜め込むと容量を圧迫するため、確認が済んだらその場で消す。

---

## Notionのスクリーンショットの撮り方

Works用の画像にNotion画面を使うときの手順（Notion公式サイトの手法を調査した結果）。

- **画面全体を撮らない。UIの一部だけを切り取る。** 全体だと文字が潰れる。一部なら文字が読めて「整った仕組み」が伝わる
- 撮る前にサイドバーを閉じ、`⌘ +` でズームを上げ、**見せるためのサンプルデータを整える**（実データそのままではなく、埋まったステータス・きれいな日付にする）
- `⌘ + Shift + 4` → `Space` → クリックでウィンドウ単位に撮る（影付きになる）
- 加工は角丸8〜12px・控えめな影・周りに余白。CleanShot X なら撮影時に自動で付く
- **サイト側で彩度を落とすフィルタが掛かるので、撮影時はやや鮮やかめでよい**
